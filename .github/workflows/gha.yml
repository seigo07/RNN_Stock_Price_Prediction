name: Action

on: [push, pull_request]

#on:
#  pull_request:
#    types:
#      - opened
#  push:
#    branches:
#      - main
#      - feature/cicd
#  workflow_dispatch:

jobs:
  lint-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
          architecture: 'x64'
      - name: Get Python version
        run: python -V
      - name: Install dependencies
        run: pip install -r requirements.txt
#      - name: Run Python
#        run: python src/python-sample.py
      - name: Run linting
        run: |
          pylint src/sample.py
